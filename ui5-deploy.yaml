# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: com.jhah.zhrjhahvar
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: s4hanadt_BAS
          url: http://jhah.virtual.ip.dhc:443
          client: '022'
        app:
          name: ZHR_JHAH_VAR
          description: Visitor Access Request Application
          package: ZHR
          transport: DHCK943531
        exclude:
          - /test/
